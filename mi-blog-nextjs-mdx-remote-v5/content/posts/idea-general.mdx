---
title: "Idea General del proyecto"
date: "2026-07-01"
description: "Lineamiento de la tomas de decision del programa TribuSoft"
slug: "idea-general"
category: "Netcore"
tags: ["netcore", "csharp", "backend"]
published: true
featured: true
---

# 1. Nombre de la aplicacion

Se llamará **TribuSoft** empesaremos con una api web hecha en net core 10 

# 2. Instalaciond de Metcore 10

Lo primero es actualizar a la utlima vesion de visual studio 2022 es la numero **17.14.35** en mi equipo tenia la **17.11.5** 

Despues hay que descargar el SDK 10 desde el link : https://dotnet.microsoft.com/download/dotnet/10.0

Una vez instalado comprobamos que la version de SDK que tenomos intalada: 
```bash
dotnet --list-sdks
```

y para comprobar cual es la version actual por defecto:
```bash
dotnet --version
```
# 3. Recomendaciones para nombrar las tablas e indices
Mi recomendación para tu proyecto

Como estás desarrollando un sistema de facturación, información exógena DIAN, .NET 10, Entity Framework Core y buscas aplicar mejores prácticas, yo usaría siempre la segunda forma y nombraría explícitamente todos los objetos de la base de datos.

Incluso seguiría una convención consistente:

Objeto	Prefijo	Ejemplo
Primary Key	PK_	PK_Departamento
Foreign Key	FK_	FK_Municipio_Departamento
Unique	UQ_	UQ_Tercero_NumeroDocumento
Check	CK_	CK_Tercero_TipoPersona
Default	DF_	DF_Tercero_Activo
Index	IX_	IX_Municipio_CodigoDepartamento


# 4. Estructura de las tablas y entidades
La tabla en sqlserver de **Municipio** seria:
```sql
CREATE TABLE municipio
(
    codigo CHAR(5) NOT NULL,
    nombre VARCHAR(120) NOT NULL,
    codigo_departamento CHAR(2) NOT NULL,

    CONSTRAINT PK_municipio
        PRIMARY KEY (Codigo),

    CONSTRAINT FK_municipio_departamento
        FOREIGN KEY (codigo_departamento)
        REFERENCES departamento(Codigo)
);

```
La tabla en sqlserver de **Departamento** seria:

```sql
CREATE TABLE departamento
(
    codigo CHAR(2) NOT NULL,
    nombre VARCHAR(80) NOT NULL,

    CONSTRAINT PK_departamento
        PRIMARY KEY (codigo)
);
```

La tabla en sqlserver de **Terceros** seria:

```sql
CREATE TABLE tercero
(
    tipo_documento      CHAR(2)       NOT NULL,
    numero_documento    VARCHAR(20)   NOT NULL,

    primer_apellido     VARCHAR(60)   NULL,
    segundo_apellido    VARCHAR(60)   NULL,

    primer_nombre       VARCHAR(60)   NULL,
    otros_nombres       VARCHAR(120)  NULL,

    razon_social        VARCHAR(200)  NULL,

    direccion          VARCHAR(130)  NULL,

    codigo_municipio    CHAR(5)       NOT NULL,


    CONSTRAINT PK_tercero
        PRIMARY KEY (numero_documento),

    CONSTRAINT FK_tercero_municipio
        FOREIGN KEY (codigo_municipio)
        REFERENCES municipio(codigo),

    CONSTRAINT UQ_tercero_numero_documento UNIQUE (numero_documento)        

);
```

# 5 Entidades en Entity Framework Core 10
Entidad **Municipio**:

```csharp
public class Municipio
{
    public string Codigo { get; set; } = null!;

    public string Nombre { get; set; } = null!;

    public string CodigoDepartamento { get; set; } = null!;

    public Departamento Departamento { get; set; } = null!;
}
```
Configuracion

```csharp
builder.HasKey(x => x.Codigo);

builder.Property(x => x.Codigo)
       .HasMaxLength(5)
       .IsFixedLength();

builder.Property(x => x.CodigoDepartamento)
       .HasMaxLength(2)
       .IsFixedLength();
```



**Nota** No es necesario crear una columna id autoincremental y tomamos los codigos que utiliza la Dian

Recomendacion para campos por valores por defecto :
forma corta:
```sql
Activo BIT DEFAULT(1)
```

Forma profesional :
```sql
CONSTRAINT DF_Empleado_Activo
DEFAULT(1)
FOR Activo
```

**Nota** los nombres de las tablas en postgresql deben ser en minuscula y separadao con **_** guion abajo
🛠️ El secreto para conectarlo con .NET 10 (Sin ensuciar tu código)
Si usas la estructura anterior (snake_case), no tienes por qué castigar tu código de C# escribiendo propiedades en minúsculas.

El proveedor oficial de Postgres para Entity Framework Core (Npgsql) incluye una solución elegante. Solo debes configurar la convención de nombres en tu DbContext para que haga la traducción automática de PascalCase (C#) a snake_case (Postgres).

1. Instala el paquete NuGet de extensiones de nombres:

```bash
dotnet add package EFCore.NamingConventions
```

2. Configura tu DbContext en .NET 10:
Al registrar tu contexto (ya sea en el Program.cs o en el OnConfiguring), añades .UseSnakeCaseNamingConvention():

```csharp
protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
{
    optionsBuilder
        .UseNpgsql("TuCadenaDeConexion")
        .UseSnakeCaseNamingConvention(); // <--- ¡Aquí está la magia!
}
```
3. Tu Entidad en C# queda perfecta y limpia:
Gracias a esa línea, EF Core sabe que cuando en C# preguntas por PrimerNombre, en Postgres debe buscar primer_nombre.

```csharp
public class Tercero 
{
    public long Id { get; set; }
    public string Nit { get; set; } = null!;
    public string PrimerNombre { get; set; } = null!; // Se mapea automáticamente a primer_nombre
    public string? SegundoNombre { get; set; }
    public string RazonSocial { get; set; } = "";
    // ...
}
```
**CONCLUSION**
manejare sqlserver con las convencion de postgresql y el codigo en csharp quedaria:
```csharp
protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
{
    // Supongamos que lees el tipo de motor desde tu appsettings.json
    string motor = Configuration["DatabaseProvider"]; 

    if (motor == "PostgreSQL")
    {
        optionsBuilder.UseNpgsql(Configuration.GetConnectionString("PostgresConnection"));
    }
    else if (motor == "SqlServer")
    {
        optionsBuilder.UseSqlServer(Configuration.GetConnectionString("SqlServerConnection"));
    }

    // ¡La magia universal! Aplica snake_case automáticamente para CUALQUIER motor
    optionsBuilder.UseSnakeCaseNamingConvention(); 
}
```

## Estructura en postgresql 18 ##
```sql
-- ===========================================
-- Tabla: departamento
-- ===========================================

CREATE TABLE departamento
(
    codigo CHAR(2) NOT NULL,
    nombre VARCHAR(80) NOT NULL,

    CONSTRAINT pk_departamento
        PRIMARY KEY (codigo)
);

-- ===========================================
-- Tabla: municipio
-- ===========================================

CREATE TABLE municipio
(
    codigo CHAR(5) NOT NULL,
    nombre VARCHAR(120) NOT NULL,
    codigo_departamento CHAR(2) NOT NULL,

    CONSTRAINT pk_municipio
        PRIMARY KEY (codigo),

    CONSTRAINT fk_municipio_departamento
        FOREIGN KEY (codigo_departamento)
        REFERENCES departamento(codigo)
);

-- ===========================================
-- Tabla: tercero
-- ===========================================

CREATE TABLE tercero
(
    tipo_documento      CHAR(2)      NOT NULL,
    numero_documento    VARCHAR(20)  NOT NULL,

    primer_apellido     VARCHAR(60),
    segundo_apellido    VARCHAR(60),

    primer_nombre       VARCHAR(60),
    otros_nombres       VARCHAR(120),

    razon_social        VARCHAR(200),

    direccion           VARCHAR(130),

    codigo_municipio    CHAR(5)      NOT NULL,

    CONSTRAINT pk_tercero
        PRIMARY KEY (numero_documento),

    CONSTRAINT fk_tercero_municipio
        FOREIGN KEY (codigo_municipio)
        REFERENCES municipio(codigo),

    CONSTRAINT uq_tercero_numero_documento
        UNIQUE (numero_documento)
);
```